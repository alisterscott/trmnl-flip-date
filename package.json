{
  "name": "trmnl-flip-date",
  "version": "1.0.0",
  "description": "1. `brew install rbenv` 2. `brew install firefox@nightly` 3. `brew install ImageMagick` 4. `rbenv init` 5. `rbenv local` 6. `bundler install` 7. `npm install` 8. `npx playwright install` 9. `APP_ENV=production trmnlp serve`",
  "homepage": "https://github.com/alisterscott/trmnl-flip-date#readme",
  "bugs": {
    "url": "https://github.com/alisterscott/trmnl-flip-date/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alisterscott/trmnl-flip-date.git"
  },
  "license": "MIT",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {},
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.3.0",
    "playwright": "^1.58.2"
  }
}
